# Secrets
.env
.env.*
*.pem
*.key
*.p12
*.pfx

# Python
__pycache__/
*.pyc
.venv/

# Local Claude Code config (settings.local.json can contain API keys)
.claude/

# macOS
.DS_Store

# Logs
*.log

# Optional local scratch files
scratch/

# Local Delphi config and key registry (contain secrets)
smoke-config.json
keys.json

# Local analysis caches (contain user PII — never commit)
*.cache.json

# Analysis output (contains customer PII — never commit)
out/
